// import fs from 'fs';g

export const parseItems = (filePath: string) => {
	return filePath;
	// const file = fs.readFileSync(filePath, 'utf8');
	// const items = file.split('\n');
	// const parsedItems = items.map((item) => {
	// 	const itemObject = JSON.parse(item);
	// 	return itemObject;
	// });
	// return parsedItems;
};
